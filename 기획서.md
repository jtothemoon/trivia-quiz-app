# 트리비아 멀티플레이 퀴즈 대결 프로젝트

## 📋 프로젝트 개요

실시간 친구와 퀴즈 대결을 할 수 있는 멀티플레이어 퀴즈 게임
- 실시간 1:1 또는 다중 플레이어 퀴즈 대결
- 카테고리별 랭킹 시스템
- 친구 매칭 및 방 생성 기능

## 🛠 기술 스택

### Frontend
- React 18
- Socket.io-client
- React Router v6
- Axios
- CSS Modules (or Styled-components)

### Backend
- Node.js (v18+)
- Express
- Socket.io
- Axios (Trivia API 호출)
- Firebase Admin SDK

### Database & External APIs
- Firebase Realtime Database (랭킹, 유저 데이터)
- Open Trivia Database API (퀴즈 문제)

### DevOps
- Docker & Docker Compose
- GitHub Actions (CI/CD)
- AWS ECR (이미지 저장소)
- AWS App Runner or ECS/Fargate

## 📁 프로젝트 구조

```
trivia-multiplayer/
├── frontend/
│   ├── Dockerfile
│   ├── package.json
│   ├── public/
│   └── src/
│       ├── components/
│       │   ├── Quiz/
│       │   │   ├── QuizCard.jsx
│       │   │   ├── QuestionDisplay.jsx
│       │   │   └── AnswerButton.jsx
│       │   ├── Room/
│       │   │   ├── RoomList.jsx
│       │   │   ├── RoomCreate.jsx
│       │   │   └── WaitingRoom.jsx
│       │   ├── Ranking/
│       │   │   ├── RankingBoard.jsx
│       │   │   └── UserScore.jsx
│       │   └── Common/
│       │       ├── Header.jsx
│       │       ├── Loading.jsx
│       │       └── ErrorBoundary.jsx
│       ├── pages/
│       │   ├── Home.jsx
│       │   ├── GameRoom.jsx
│       │   ├── Ranking.jsx
│       │   └── Profile.jsx
│       ├── services/
│       │   ├── socket.js
│       │   ├── api.js
│       │   └── firebase.js
│       ├── hooks/
│       │   ├── useSocket.js
│       │   ├── useQuiz.js
│       │   └── useRanking.js
│       ├── context/
│       │   ├── AuthContext.jsx
│       │   └── GameContext.jsx
│       ├── utils/
│       │   ├── helpers.js
│       │   └── constants.js
│       ├── App.jsx
│       └── index.js
│
├── backend/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
│       ├── config/
│       │   ├── firebase.js
│       │   └── constants.js
│       ├── routes/
│       │   ├── quiz.routes.js
│       │   ├── ranking.routes.js
│       │   └── user.routes.js
│       ├── socket/
│       │   ├── index.js
│       │   ├── gameHandler.js
│       │   ├── roomHandler.js
│       │   └── matchHandler.js
│       ├── services/
│       │   ├── triviaAPI.js
│       │   ├── firebaseService.js
│       │   ├── gameService.js
│       │   └── rankingService.js
│       ├── middleware/
│       │   ├── auth.js
│       │   ├── errorHandler.js
│       │   └── validator.js
│       ├── models/
│       │   ├── Game.js
│       │   ├── Room.js
│       │   └── User.js
│       ├── utils/
│       │   ├── helpers.js
│       │   └── logger.js
│       └── server.js
│
├── docker-compose.yml
├── .github/
│   └── workflows/
│       ├── deploy-backend.yml
│       └── deploy-frontend.yml
├── .gitignore
├── .env.example
└── README.md
```

## 🔑 핵심 기능 명세

### 1. 사용자 인증 & 프로필
- 닉네임 기반 간단 인증
- 프로필 생성 및 수정
- 전적 조회

### 2. 방 생성 & 매칭
- 공개방/비공개방 생성
- 방 코드로 입장
- 빠른 매칭 (랜덤 매칭)
- 대기실 (참가자 목록, 준비 상태)

### 3. 퀴즈 게임
- 카테고리 선택 (역사, 과학, 스포츠, 엔터테인먼트 등)
- 난이도 선택 (Easy, Medium, Hard)
- 객관식 4지선다
- 제한 시간 (문제당 15초)
- 실시간 점수 표시
- 정답/오답 즉시 피드백

### 4. 점수 계산 시스템
```
기본 점수 = 100점
시간 보너스 = (남은 시간 / 15) * 50
난이도 배수 = Easy(1x), Medium(1.5x), Hard(2x)
최종 점수 = (기본 점수 + 시간 보너스) * 난이도 배수
```

### 5. 랭킹 시스템
- 전체 랭킹
- 카테고리별 랭킹
- 주간/월간 랭킹
- 친구 랭킹

## 🔌 Socket.io 이벤트 설계

### Client → Server

```javascript
// 방 관련
'room:create'     // { nickname, category, difficulty, isPrivate }
'room:join'       // { roomId, nickname }
'room:leave'      // { roomId }
'room:ready'      // { roomId }

// 게임 관련
'game:start'      // { roomId }
'game:answer'     // { roomId, questionId, answer, timeSpent }
'game:next'       // { roomId }

// 매칭 관련
'match:quick'     // { nickname, category }
'match:cancel'    // {}
```

### Server → Client

```javascript
// 방 관련
'room:created'    // { roomId, roomInfo }
'room:joined'     // { players }
'room:player-joined'  // { player }
'room:player-left'    // { playerId }
'room:ready-status'   // { playerId, isReady }

// 게임 관련
'game:started'        // { gameId, firstQuestion }
'game:question'       // { question, questionNumber, totalQuestions }
'game:answer-result'  // { playerId, isCorrect, score, correctAnswer }
'game:scores'         // { scores: [{playerId, score, nickname}] }
'game:finished'       // { finalScores, winner }

// 매칭 관련
'match:found'     // { roomId, opponent }
'match:failed'    // { message }

// 에러
'error'          // { message, code }
```

## 🔥 Firebase Realtime Database 스키마

```json
{
  "users": {
    "userId1": {
      "nickname": "Player1",
      "totalGames": 150,
      "totalWins": 89,
      "totalScore": 45000,
      "bestScore": 850,
      "createdAt": 1234567890,
      "lastActive": 1234567890
    }
  },
  "rankings": {
    "overall": {
      "userId1": {
        "nickname": "Player1",
        "score": 45000,
        "wins": 89,
        "rank": 1
      }
    },
    "categories": {
      "science": {
        "userId1": {
          "nickname": "Player1",
          "score": 12000,
          "rank": 3
        }
      }
    },
    "weekly": {
      "2024-W45": {
        "userId1": {
          "nickname": "Player1",
          "score": 5000,
          "wins": 12
        }
      }
    }
  },
  "games": {
    "gameId1": {
      "roomId": "room123",
      "category": "science",
      "difficulty": "medium",
      "players": {
        "userId1": {
          "nickname": "Player1",
          "score": 450,
          "answers": [true, false, true]
        }
      },
      "startedAt": 1234567890,
      "finishedAt": 1234567891,
      "winner": "userId1"
    }
  }
}
```

## 🌐 REST API 엔드포인트

### Users
```
GET    /api/users/:userId          # 유저 정보 조회
POST   /api/users                  # 유저 생성
PUT    /api/users/:userId          # 유저 정보 수정
GET    /api/users/:userId/stats    # 유저 통계
```

### Ranking
```
GET    /api/rankings/overall       # 전체 랭킹
GET    /api/rankings/category/:category  # 카테고리별 랭킹
GET    /api/rankings/weekly        # 주간 랭킹
GET    /api/rankings/friends/:userId     # 친구 랭킹
```

### Quiz
```
GET    /api/quiz/categories        # 카테고리 목록
GET    /api/quiz/questions         # 퀴즈 문제 가져오기
                                    # ?category=science&difficulty=medium&amount=10
```

### Games
```
GET    /api/games/:gameId          # 게임 결과 조회
GET    /api/games/user/:userId     # 유저 게임 히스토리
```

## 🐳 Docker 설정

### frontend/Dockerfile
```dockerfile
# Build stage
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Production stage
FROM nginx:alpine
COPY --from=builder /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### backend/Dockerfile
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["node", "src/server.js"]
```

### docker-compose.yml
```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - FIREBASE_CONFIG=${FIREBASE_CONFIG}
      - TRIVIA_API_URL=https://opentdb.com/api.php
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: npm run dev

  frontend:
    build: ./frontend
    ports:
      - "3001:3001"
    environment:
      - REACT_APP_API_URL=http://localhost:3000
      - REACT_APP_SOCKET_URL=http://localhost:3000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm start
    depends_on:
      - backend
```

## 🚀 개발 시작하기

### 1. 프로젝트 초기 설정

```bash
# 프로젝트 루트 생성
mkdir trivia-multiplayer
cd trivia-multiplayer

# Backend 설정
mkdir backend
cd backend
npm init -y
npm install express socket.io cors dotenv axios firebase-admin
npm install -D nodemon

# Frontend 설정
cd ..
npx create-react-app frontend
cd frontend
npm install socket.io-client axios react-router-dom
```

### 2. 환경 변수 설정

`.env.example` 파일 생성:
```env
# Backend
NODE_ENV=development
PORT=3000
TRIVIA_API_URL=https://opentdb.com/api.php

# Firebase
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY=your-private-key
FIREBASE_CLIENT_EMAIL=your-client-email
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# Frontend (React)
REACT_APP_API_URL=http://localhost:3000
REACT_APP_SOCKET_URL=http://localhost:3000
```

### 3. Firebase 설정

1. Firebase Console에서 프로젝트 생성
2. Realtime Database 활성화
3. 서비스 계정 키 다운로드
4. 환경 변수에 설정

### 4. 로컬 개발 실행

```bash
# Docker Compose로 실행
docker-compose up

# 또는 개별 실행
# Backend
cd backend
npm run dev

# Frontend (새 터미널)
cd frontend
npm start
```

## 📝 개발 순서 (권장)

### Phase 1: 기본 구조 (1-2일)
1. ✅ 프로젝트 구조 생성
2. ✅ Backend 서버 기본 설정 (Express + Socket.io)
3. ✅ Frontend 라우팅 설정
4. ✅ Firebase 연동
5. ✅ Trivia API 연동 테스트

### Phase 2: 사용자 & 방 기능 (2-3일)
1. ✅ 닉네임 입력 및 사용자 생성
2. ✅ 방 생성/입장 UI
3. ✅ 대기실 구현
4. ✅ Socket.io 방 관리 로직
5. ✅ 참가자 상태 동기화

### Phase 3: 퀴즈 게임 (3-4일)
1. ✅ 퀴즈 문제 로딩 로직
2. ✅ 게임 시작/진행 로직
3. ✅ 타이머 구현
4. ✅ 정답 제출 및 검증
5. ✅ 점수 계산 시스템
6. ✅ 게임 결과 화면

### Phase 4: 랭킹 시스템 (2-3일)
1. ✅ Firebase에 게임 결과 저장
2. ✅ 랭킹 계산 로직
3. ✅ 랭킹 조회 API
4. ✅ 랭킹 UI 구현
5. ✅ 카테고리별/주간 랭킹

### Phase 5: 배포 준비 (2-3일)
1. ✅ Docker 이미지 최적화
2. ✅ GitHub Actions 워크플로우 작성
3. ✅ AWS 환경 설정 (ECR, App Runner/ECS)
4. ✅ 환경 변수 관리
5. ✅ 모니터링 설정

### Phase 6: 테스트 & 개선 (ongoing)
1. ✅ 단위 테스트 작성
2. ✅ 통합 테스트
3. ✅ 부하 테스트
4. ✅ 버그 수정 및 최적화

## 🧪 테스트 계획

### Backend 테스트
```bash
npm install -D jest supertest
```

- Socket.io 이벤트 테스트
- API 엔드포인트 테스트
- Firebase 서비스 테스트
- Trivia API 모킹 테스트

### Frontend 테스트
```bash
npm install -D @testing-library/react @testing-library/jest-dom
```

- 컴포넌트 렌더링 테스트
- Socket 연결 테스트
- 게임 플로우 테스트

## 🔐 보안 고려사항

1. **환경 변수 관리**
   - `.env` 파일은 절대 커밋 금지
   - AWS Secrets Manager 사용 고려

2. **Rate Limiting**
   - API 요청 제한 (express-rate-limit)
   - Socket 연결 제한

3. **입력 검증**
   - 닉네임 길이 제한 (2-20자)
   - 특수문자 필터링
   - XSS 방지

4. **Firebase 보안 규칙**
```json
{
  "rules": {
    "users": {
      "$userId": {
        ".read": true,
        ".write": "$userId === auth.uid"
      }
    },
    "rankings": {
      ".read": true,
      ".write": false
    }
  }
}
```

## 📊 성능 최적화

1. **Frontend**
   - React.memo로 불필요한 리렌더링 방지
   - Code splitting (React.lazy)
   - 이미지 최적화

2. **Backend**
   - Socket.io rooms 효율적 관리
   - Firebase 쿼리 최적화
   - API 응답 캐싱

3. **Network**
   - Socket.io binary 데이터 전송
   - gzip 압축
   - CDN 사용 고려

## 🐛 디버깅 팁

### Socket.io 연결 확인
```javascript
// Client
socket.on('connect', () => console.log('Connected:', socket.id));
socket.on('disconnect', () => console.log('Disconnected'));

// Server
io.on('connection', (socket) => {
  console.log('Client connected:', socket.id);
  socket.on('disconnect', () => {
    console.log('Client disconnected:', socket.id);
  });
});
```

### Firebase 연결 확인
```javascript
const db = admin.database();
db.ref('.info/connected').on('value', (snapshot) => {
  console.log('Firebase connected:', snapshot.val());
});
```

## 📚 참고 자료

- [Socket.io Documentation](https://socket.io/docs/v4/)
- [Firebase Realtime Database](https://firebase.google.com/docs/database)
- [Open Trivia Database API](https://opentdb.com/api_config.php)
- [React Documentation](https://react.dev/)
- [Docker Documentation](https://docs.docker.com/)
- [AWS App Runner](https://docs.aws.amazon.com/apprunner/)

## 🎯 추가 기능 아이디어 (Optional)

- 🎨 커스텀 아바타/프로필 이미지
- 💬 인게임 채팅
- 🏆 업적/뱃지 시스템
- 👥 친구 시스템
- 📱 PWA 지원 (모바일 앱처럼)
- 🎵 효과음/배경음악
- 🌍 다국어 지원
- 🎁 일일 보상 시스템

## 🤝 기여 가이드

1. Feature 브랜치 생성
2. 변경사항 커밋
3. Pull Request 생성
4. 코드 리뷰 후 머지

---

**프로젝트 시작일**: 2024-11-11
**예상 완료일**: 2024-12-01 (3주)
**목표**: MVP 배포 및 피드백 수집